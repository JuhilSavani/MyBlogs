<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap");
      * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: "Rubik";
        letter-spacing: 0.5px;
        word-spacing: 1.25px;
      }
      body {
        background-color: #666;
      }
      :root {
        --title-size: 4rem;
        --sub-title-size: 2.5rem;
        --heading-1-size: 2rem;
        --heading-2-size: 1.75rem;
        --heading-3-size: 1.5rem;
        --body-size: 1.25rem;
      }
      .container {
        border-radius: 10px;
        background-color: #eeeeee;
        padding: 2.5rem;
        margin: 5rem auto;
        width: 85%;
        position: relative;
        box-shadow: inset 0 0 16px rgba(0, 0, 0, 1);
      }
      .container > h1 {
        font-size: var(--title-size);
        margin-bottom: 2rem;
        text-transform: capitalize;
      }
      .blog:not(:last-child) {
        border-bottom: 0.8px solid #666;
      }
      .blog > * {
        font-size: var(--body-size);
      }
      .date {
        display: block;
        margin-bottom: 1rem;
        font-size: 16px;
        color: #666;
      }
      .tag {
        display: block;
        margin-top: 1rem;
        font-size: 16px;
        color: #666;
      }
      .title {
        font-size: var(--heading-2-size);
        margin: 3rem 0 1rem 0;
      }
      .edit, .delete {
        margin: 2rem 2rem 2rem 0;
        border-radius: 8px;
        padding: 0.5rem;
        font-size: var(--heading-3-size);
        color: #333;
        border: 6px solid #666;
        background-color: #FFDA78;
        width: 100px;
      }
      .delete{
        background-color: #FF6969;
      }
      .content {
        white-space: pre-wrap;
      }
      .container div:nth-child(2){
        width: 100px;
        height: 100px;
        position: absolute;
        top: 35px;
        right: 50px;
        border-radius: 50%;
        border: 6px solid #436850;
        background-color: #40A578;
        box-shadow: 0 0 8px rgba(0, 0, 0, 0.5);
        transition: 0.4s ease;
        cursor: pointer;
        user-select: none;
      }
      .container div:nth-child(2) span{
        position: absolute;
        top: 50%;
        left: 50%;
        font-size: 5rem;
        font-weight: 500;
        color: #fff;
        transform: translate(-50%, -53%);
      }
      .no-blog{
        box-shadow: 0 0 8px rgba(0, 0, 0, 0.5);
        font-size: 1.25rem;
        border: 4px solid #555;
        border-radius: 10px;
        width: fit-content;
        padding: 0.5rem 1rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>My Blogs</h1>
      <div><span>+</span></div>
      <% if(blogs.length){ %>
        <% for (let b of blogs){ %>
        <div class="blog" id="<%=b.id%>">
          <h2 class="title"><%=b.title%></h2>
          <span class="date"><%=b.date%></span>
          <p class="content"><%= b.content %></p>
          <p class="tag">On: <%=b.tag%></p>
          <button class="delete">Delete</button>
          <button class="edit">Edit</button>
        </div>
        <% } %>
      <% }else{%>
          <p class="no-blog">You have No Blogs Yet, Let's Create Some... <br/>by Clicking on That Green Button at Corner</p>
      <% } %>
        
    </div>
    <script>
      document.addEventListener('DOMContentLoaded', ()=>{
        document.querySelectorAll('.edit').forEach((button) =>{
          button.addEventListener('click', ()=>{
            window.location.href = `/edit/${button.parentNode.id}`;
          });
        });
        document.querySelectorAll('.delete').forEach((button) =>{
          button.addEventListener('click', ()=>{
            window.location.href = `/del/${button.parentNode.id}`
          });
        });
        document.querySelector('.container div:nth-child(2)').addEventListener('click', ()=>{
          window.location.href = `/new`;
        });
      });
    </script>
  </body>
</html>
